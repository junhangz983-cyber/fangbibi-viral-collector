interface:
  display_name: "方比比爆款采集"
  short_description: "导入第三方数据、去重并入库飞书"
  default_prompt: "Use $fangbibi-viral-collector to import a third-party social media export, clean and deduplicate the viral posts, then sync the new records to Feishu Base."
